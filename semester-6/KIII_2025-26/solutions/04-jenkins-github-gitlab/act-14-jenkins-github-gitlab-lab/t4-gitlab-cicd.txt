.gitlab-ci.yml file in root:

stages:
  - build

build_image:
  stage: build
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind

  variables:
    DOCKER_TLS_CERTDIR: ""

  script:
    - echo "Logging in to Docker registry"
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY

    - echo "Building Docker image"
    - docker build -t $CI_REGISTRY_IMAGE:latest .

    - echo "Pushing Docker image"
    - docker push $CI_REGISTRY_IMAGE:latest

2. 
CI_REGISTRY
CI_REGISTRY_IMAGE
CI_REGISTRY_PASSWORD
CI_REGISTRY_USER

3. 
Evidence of pipeline run (from GitLab Pipelines view):
Pipeline ID: #152336941
Pipeline source: push
Branch: main
Pipeline status: passed
Pipeline duration: 1m 22s

Jobs shown in pipeline:
Job name: docker-build
Job status: passed
Stage: build
Job duration: 1m 22s