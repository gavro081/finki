1. .github/workflows/combined-pipeline.yml
name: Combined Lint and Docker Build/Push

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  super-lint:
    name: run super-linter
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Lint Code Base
        uses: github/super-linter@v5
        env:
          VALIDATE_ALL_CODEBASE: false
          DEFAULT_BRANCH: "main"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docker:
    name: Build and Push Docker Image
    needs: [super-lint]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/my-node-app:latest

2. 
The docker job successfully declares its dependency on the linting job via:
needs: [super-lint]

3.
- DOCKERHUB_USERNAME
- DOCKERHUB_TOKEN

4.

Run summary: Combined Lint and Docker Build/Push
Status: Success 
Triggered via push by user

Jobs shown in Actions graph:
super-lint  -> docker

Annotations:
0 errors, 0 warnings

Job Output Details:
-> super-lint: All linters passed successfully.
-> docker: Image successfully built and pushed to docker.io/x/my-node-app:latest.